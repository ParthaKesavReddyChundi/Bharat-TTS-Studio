# ============================================================
# Bharat TTS Studio — Pinned Dependencies
# Python >= 3.10 required
#
# IMPORTANT: PyTorch must be installed with CUDA support.
# The torch/torchaudio lines below are for CUDA 12.1 (RTX 4060).
# Install via:
#   pip install torch==2.3.1+cu121 torchaudio==2.3.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
# Then install the rest:
#   pip install -r requirements.txt
# ============================================================

# ---- GUI -------------------------------------------------------
PySide6==6.7.2

# ---- ML / Inference --------------------------------------------
# NOTE: torch + torchaudio installed separately with CUDA wheel (see above)
torch>=2.3.1
torchaudio>=2.3.1
transformers==4.44.2
huggingface_hub==0.24.6
accelerate==0.33.0
datasets==2.21.0

# Optional acceleration (guarded import — app runs without this)
# onnxruntime-gpu==1.18.1   # uncomment if you want ONNX path

# ---- Coqui TTS (for XTTS v2 adapter — Phase 6) ----------------
# TTS==0.22.0   # uncomment in Phase 6

# ---- Audio -----------------------------------------------------
soundfile==0.12.1
sounddevice==0.4.7
librosa==0.10.2
numpy==1.26.4
scipy==1.14.0

# Optional pitch-preserving time-stretch (guarded import)
# pyrubberband==0.3.0   # uncomment if rubberband binary is installed

# ---- Visualization ---------------------------------------------
pyqtgraph==0.13.7
matplotlib==3.9.1

# ---- System / Utilities ----------------------------------------
psutil==6.0.0
PyYAML==6.0.2
python-dateutil==2.9.0
regex==2024.7.24

# Optional NVIDIA GPU stats (guarded import)
pynvml==11.5.0

# ---- Text Normalization ----------------------------------------
num2words==0.5.13
# indic-num2words — install from source if pip version unavailable:
#   pip install git+https://github.com/indic-num2words/indic-num2words.git

# ---- Dev / Testing ---------------------------------------------
pytest==8.3.2
pytest-qt==4.4.0
